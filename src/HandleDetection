class HandleDetection {
    constructor(component) {
        this.component = component;
    }

    handleDetection = () => {
        console.log('Run Detection', this.component.state);
    }
}

export default HandleDetection;